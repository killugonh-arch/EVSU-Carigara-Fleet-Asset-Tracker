from django.db import migrations, models


class Migration(migrations.Migration):

    dependencies = [
        ('assets', '0003_asset_maintenance_schedule'),
    ]

    operations = [
        migrations.AlterField(
            model_name='asset',
            name='status',
            field=models.CharField(
                choices=[
                    ('buying',      'Buying'),
                    ('active',      'Active'),
                    ('maintenance', 'In Maintenance'),
                    ('inactive',    'Inactive'),
                    ('disposed',    'Disposed'),
                ],
                db_index=True,
                default='active',
                max_length=20,
            ),
        ),
        # Rename any existing 'retired' rows to 'inactive'
        migrations.RunSQL(
            sql="UPDATE assets_asset SET status = 'inactive' WHERE status = 'retired';",
            reverse_sql="UPDATE assets_asset SET status = 'retired' WHERE status = 'inactive';",
        ),
    ]